# watch.py is stdlib only (urllib for the Yahoo Finance fetch) and stays
# that way -- it's a shared library, not a standalone tool.
#
# The default provider (claude_chat) is stdlib only: it drives claude.ai
# through an authenticated Chrome tab over the DevTools Protocol
# (discovery/providers/cdp.py), so no SDK and no API key are needed.

# Optional: only for DISCOVERY_PROVIDER=anthropic (direct API, pay-per-token).
# Imported lazily inside discovery/providers/anthropic_provider.py.
# anthropic>=0.120.2

# Optional: only for DISCOVERY_PROVIDER=openai. Install it yourself if you
# want that provider -- it is imported lazily, so the default path works
# without it.
# openai>=2.9.0

# Optional: only for the youtube collector's transcript fetch (video search
# itself is a plain YOUTUBE_API_KEY REST call, no SDK needed). Imported
# lazily inside discovery/collectors/youtube.py; without it, that collector
# logs and skips every video instead of failing the cycle.
# youtube-transcript-api>=1.2.4

# Required only for `python -m app ui` (the Observatory, observatory/, step-13
# task 2) and for running test_observatory.py -- the ONE sanctioned new
# dependency this repo takes on (see PROJECT_STATE.md). Imported lazily,
# inside observatory/ and discovery/__main__.py's `ui` command handler only;
# discovery/ and test_discovery.py stay importable/green without it.
# datasette>=0.65
